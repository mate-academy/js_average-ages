'use strict';

/**
 * Implement sum function:
 * Function gets 2 numbers and return it's sum
 *
 * @param {number} a
 * @param {number} b
 *
 * @return {number}
 *
 * sum(1, 2) === 3
 * sum(1, 11) === 12
 *
 */

function sum(a, b) {
  // write code here
  return a + b;
}

module.exports = sum;
